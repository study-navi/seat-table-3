# Google自動バックアップの初回設定（教室3）

この手順は **seat-table-classroom-3** 専用です。他の教室では、それぞれのリポジトリの手順とスクリプトを使ってください。別教室のURLや合言葉を使い回すと、データが混ざったり上書きされたりします。

先生が座席表アプリを使うときは、Googleログインは不要です。設定作業だけ、管理者のGoogleアカウントで一度行います。

## 1. Apps Script を作る

1. [Google Apps Script](https://script.google.com/) を開く
2. 「新しいプロジェクト」を作る
3. プロジェクト名を `座席表バックアップ-教室3` などにする
4. このリポジトリの `google-apps-script/Code.gs` の内容を、エディタの `Code.gs` に貼り付ける
5. `google-apps-script/appsscript.json` がある場合は、エディタ左の歯車「プロジェクトの設定」→「appsscript.json マニフェストファイルをエディタで表示する」をオンにし、中身を合わせる
6. `CLASSROOM_ID` が `seat-table-classroom-3` のままか確認する（書き換えない）

## 2. 合言葉を入れる

1. エディタ左の歯車「プロジェクトの設定」
2. 「スクリプト プロパティ」に行を追加する
   - プロパティ：`SHARED_TOKEN`
   - 値：教室3専用の合言葉（他教室と同じにしない）
3. 保存する

合言葉はソースコードやGitHubには書かないでください。

## 3. 保存先を作る

1. エディタ上部の関数一覧で `initialSetup` を選ぶ
2. 「実行」を押す
3. 初回は Google ドライブとスプレッドシートの許可を承認する
4. 実行が成功すると、Drive に次が作られます
   - フォルダ：`座席表バックアップ-seat-table-classroom-3`
   - スプレッドシート：`座席表バックアップ管理-seat-table-classroom-3`

実データ（座席表のJSON）はフォルダ内のファイルに保存されます。スプレッドシートには教室ID・日時・ファイルIDなどの管理情報だけが入ります。

## 4. Webアプリとして公開する

1. 右上「デプロイ」→「新しいデプロイ」
2. 種類は「ウェブアプリ」
3. 次のようにする
   - 説明：教室3バックアップ など
   - 次のユーザーとして実行：自分
   - アクセスできるユーザー：**全員**（アプリを使う先生のGoogleログインを不要にするため）
4. 「デプロイ」し、表示されたURL（末尾が `/exec`）をコピーする

あとからコードを直したときは、「デプロイ」→「デプロイを管理」→鉛筆アイコン→「新バージョン」で更新してください。古いURLのままだと動きません。

## 5. 座席表アプリ側で設定する

1. 教室3の座席表を開く（`https://study-navi.github.io/seat-table-3/`）
2. 「設定・バックアップ」タブ
3. 「Google自動バックアップ」の「設定する」
4. WebアプリURLと、手順2の合言葉を入力して保存する

設定値はこの教室のブラウザにだけ保存されます。他の教室の画面には入りません。

## 6. 動作確認

1. 座席を少し直して数秒待つ、または「今すぐバックアップ」
2. 画面上が「Googleに自動バックアップ済み」になる
3. Drive の教室3フォルダに `seat-table-classroom-3-latest.json` がある
4. 必要なら別の端末で同じURL・合言葉を設定し、「Googleから復元」で戻せる

復元時は、確認文を読んだうえで実行してください。この端末にしかない変更は消えます。

## うまくいかないとき

- **ネット未接続**：座席表の編集は端末内保存だけで続けられます。つながったあとの変更で再試行されます
- **教室IDまたは合言葉が正しくありません**：別教室のURL／合言葉を貼っていないか確認する
- **このWebアプリは別の教室用です**：別教室のスクリプトURLを入れていませんか
- バックアップの中身（生徒名など）は画面・URL・ブラウザの開発者ツールに出さない仕様です
